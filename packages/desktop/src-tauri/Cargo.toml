[package]
name = "orbit-ai"
version = "0.1.0"
description = "Orbit AI — AI-Powered Team Coding Platform"
authors = ["Rowe Casa Organics IT"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
